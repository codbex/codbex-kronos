schema   = "ACME";
minvalue = 1;
cycles   = false;
reset_by = "select ifnull(max(id),0) + 1 as id from
        (select max(id) as id from \"acme.db.evaluation::t_evaluation_request_item\" )";
depends_on_table = "acme.db.evaluation::t_evaluation_request_item";