# name is used in docker-image-scanner-scout.yaml
name: Build - Platform

on:
  push:
    branches:
      - main

jobs:
  main:
    uses: codbex/codbex-infra/.github/workflows/platform-build.yaml@main
    with:
      platform-name: codbex-kronos
      docker-build: true
    secrets: inherit
