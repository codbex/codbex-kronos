name: "CodeQL Advanced [Push & PR]"

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '32 20 * * 0'

jobs:
  main:
    uses: codbex/codbex-infra/.github/workflows/platform-codeql-pr-push.yml@main
    secrets: inherit
    with:
