name: "Comprehensive Multi-language CodeQL Config"

query-filters:
  - exclude:
      id: java/path-injection

queries:
  # Main security and quality pack (covers all supported languages)
  - uses: security-and-quality

  # Optional: also explicitly include latest Java and JS/TS packs (they overlap but ensure latest queries)
  - uses: codeql/java-queries@latest
  - uses: codeql/javascript-queries@latest
